- if zone.errors.any?
  #error_explanation
    .alert.alert-danger
      The form contains 
      = pluralize(zone.errors.count, "error")
    %ul
      - zone.errors.full_messages.each do |msg|
        %li 
          *
          = msg
