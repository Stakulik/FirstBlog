=form_for @article do |f|
  =f.text_field :title, size: "61"
  %br
  =f.text_area :content, size: "60x12"
  minimum: 10
  %br
  -if @article.new_record?
    =f.submit "Создать"
  -else
    =f.submit "Изменить"