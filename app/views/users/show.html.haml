- provide(:title, @user.name)
%h1.h1-signed
  Информация о 
  = @user.name

%p
  User:
  = @user.name
  %br
  Email:
  = @user.email


= link_to "Изменить", edit_user_path
= link_to "Удалить", user_path, method: :delete, data: {confirm: 'Уверены?'}